import type { Metadata } from "next";

export const metadata: Metadata = { title: "Training Session" };

export default function TrainingSessionLayout({ children }: { children: React.ReactNode }) {
  return children;
}
